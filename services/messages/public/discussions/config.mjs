// Public locations only. Future server adapters keep credentials/sign-in private.
// Relative paths move with this service. Set an absolute HTTPS URL if separated.
export const config = Object.freeze({messageApiBase:'/api/messages',sender:'rick',channel:'general'});

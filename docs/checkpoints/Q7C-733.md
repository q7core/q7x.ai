# Q7C-733 recovery checkpoint

Updated: 2026-09-14 02:55 UTC. Page DEPLOYED; remaining visual QA blocked.

## CURRENT RECOVERY STATE — read this first

- Live page https://q7x.ai/discussions/; homepage link added without replacing
  original content. Current release /opt/q7x-messages/releases/20260914-q7c733-1.
- Q7C-733 remains In Progress only because mobile/final live Chrome QA could not
  be completed. Parent Q7C-732 stays open. No live agents/dispatch/integrations.
- Host Node 22 tests: 15 tests, 15 passed, 0 failed, 0 skipped, duration 301.220282ms.
- Desktop local Chrome: selection, question/viewpoint preparation, disabled run
  controls and draft reload persistence verified visually and via AX.
- Live HTTPS: all 8 served workspace assets byte-match source; homepage link exists;
  API returns 401 without token, 200 with token. Existing seven services active.
- Caddy validated/reloaded. Preserved previous service release. Caddy/homepage
  backups end in 20260914T025419Z, alongside their respective source files.
- Implementation commit 9955833; deployment/docs/checkpoint follow-up to be committed
  and pushed next. Check git log/status for exact latest SHA on resume.
- Only required remaining verification: get native Chrome AX/screenshot working;
  inspect live page at desktop and mobile sizes, verify edit-stale/update behavior
  and no-participant validation. Do not substitute unverified screenshots. Then
  record evidence and close only Q7C-733. No code/deployment changes are currently
  known necessary. Restarting/reinstalling Chrome or changing permissions is not
  authorized merely to fix the QA tool; use supported cua controls.
- Local preview process session 56936 on port 8767 can be stopped after closeout.
- Actual tool blocker: getAXState yields just window title and getScreenshot says
  Screenshot unavailable for /Applications/Google Chrome.app, even after fresh
  app binding/kernel reset/new window. No forbidden automation workaround used.

Older phase notes below are historical, not the current deployment state.

## Latest phase — supersedes initial snapshot below

Implementation committed locally as 9955833. Independent review: no actionable
findings; reviewer independently passed 4 draft/client checks and static path
checks. Added DISCUSSIONS.md contract/deployment doc and
deploy/enable-discussions.py (uncommitted as of this note). New release staged at
/opt/q7x-messages/releases/20260914-q7c733-1; host npm checks running in session 22184.
Next: inspect those results, activate the new preserved release, then enable route
and homepage link. Current remains the Q7C-731 release until activation.

Chrome native AX/screenshot unavailable even after reacquiring app, resetting cua
session and trying a fresh window. Desktop QA above is verified; MOBILE QA IS NOT.
Continue deployment/HTTP checks, preserve exact limit and leave child open until
visual QA can finish. Do not substitute fabricated screenshots or use forbidden
browser automation. No user reset/automation requested or performed.

Local checks now 15/15 passed (includes public-static allowlist/auth regression).
Desktop Chrome visually inspected: layout clean; Codex+Hermes selected; question
and viewpoint typed; Prepare shows Rick's own note with correct participants and
Not sent label; reload restored the local draft. Start/Pause/Stop disabled in AX.
Mobile QA pending: Chrome native screenshot became unavailable after opening
DevTools/device toolbar shortcuts. Reacquire Chrome with cua.getApp if needed.
Local static server active session 56936 on port 8767. No remote page changes yet.

Added clients.mjs, config.mjs, draft.mjs, app.mjs under public/discussions;
workspace.mjs serves a strict public file allowlist at /discussions/. api.mjs now
calls it before bearer auth (only public static files); existing API remains gated.
package.json test script includes test/discussions.test.mjs. All 14 tests passed
locally (10 existing HTTP/config + 4 new draft/placeholder contract), zero failures.
No visual QA or deployment yet. Next: start local preview with
`python3 -m http.server 8767 --bind 127.0.0.1 --directory services/messages/public`,
then use native Chrome through cua to inspect http://127.0.0.1:8767/discussions/.
Add/check static route allowlist coverage, inspect desktop/mobile interactions,
then stage/deploy per steps below. No live agents or browser API calls exist.

### Historical initial snapshot (preserved for continuity)

## Scope and records

- Child https://linear.app/q7core/issue/Q7C-733 — Create agent discussion landing
  page with portable client placeholders; General Admin and Infrastructure;
  In Progress, reverified before implementation.
- Parent https://linear.app/q7core/issue/Q7C-732 — future dispatcher/live adapters;
  remains open. Do not close it for the landing page.
- Q7C-731 API is already deployed: https://q7x.ai/api/messages and public docs
  https://q7x.ai/api/messages/docs. Source main commit 0e974e3.
- Authorized: publish a simple polished page with selectable Codex, Claude Code,
  Gemini CLI, Hermes and OpenClaw; question + Rick's viewpoint; working preparation
  and clearly local draft persistence; honest empty outcomes and disabled live
  controls. Common message/sender contract MUST reuse id, sender, channel, text,
  created_at; outgoing drafts use sender/channel/text. Plain functions/adapters,
  no DI container/framework. No fake replies/connections/runs/consensus. No live
  integrations, dispatcher, timers, model calls, costs or routing work here.

## Checkout and exact current state

Worktree: `/Users/rproemer/.codex/worktrees/c684/q7x.ai`
Branch: `codex/q7c-733-discussion-page`
Base: `0e974e3a81effbc02f72c5960d560ff0a8d8bcc9`.
Successful `git pull --ff-only origin main` before editing; branch created.
New work is UNCOMMITTED and NOT deployed. Preserve all files.

Files written:
- `services/messages/DISCUSSIONS-PLAN.md`: verified issue and implementation plan.
- `services/messages/public/discussions/index.html`: complete page skeleton/copy,
  semantic composer, participant container, draft/empty/outcome/run controls.
- `services/messages/public/discussions/styles.css`: desktop/mobile navy/cyan UI.
- `services/messages/public/discussions/tokens.css`: palette.
- `services/messages/public/discussions/favicon.svg`: local favicon.
- This checkpoint.

Not written yet: app.mjs, clients.mjs, draft contract functions, static serving
allowlist, contract checks, deployment changes, final operations/evidence.
The current HTML references app.mjs, which does NOT exist yet. Do not claim the
page functions or publish this incomplete slice.

## Verified investigation; tests/deployment pending

- Read AGENTS.md and message operations runbook; Linear scope reread after no-DI
  clarification. Read design skill and references; design uses product register.
- Public q7x homepage inspected via HTTPS and visually in external Chrome. It is
  a simple black coming-soon page with q7x mark/contact. Preserve it, add a link.
- Remote homepage source: `/home/ubuntu/q7x/caddy/index.html`, single-file Docker
  bind mount at `/srv/www/index.html`. Preserve inode when editing.
- Caddy source `/home/ubuntu/q7x/caddy/Caddyfile`; container q7x-caddy. Current
  service q7x-messages active. No remote changes performed for Q7C-733.
- Browser extension control for chrome unavailable; native Google Chrome control
  through cua works. Tool session bindings: chromeApp = cua.getApp('com.google.Chrome').
  New task tab https://q7x.ai is open in current incognito window. Use native
  keyboard/accessibility through cua only, not AppleScript or Playwright.
- No Q7C-733 tests/build/desktop-mobile page QA or deployment performed yet.
- No blockers besides finite usage; user asked checkpoint and continue, not reset.

## Exact next steps

1. Complete plain ES modules under public/discussions. clients.mjs exports shared
   definitions for five clients with status='not_connected' and false live
   capabilities; small placeholder functions reject send/start. draft.mjs validates
   saved inputs and prepares sender/channel/text opening message (no invented IDs).
   app.mjs renders accessible checkbox cards, validates question/selection, shows
   Rick's own prepared note, saves local draft with honest status, and marks edits
   stale until prepared again. Do not call the API from browser yet.
2. Add explicit static file allowlist for /discussions/ in existing message service
   (e.g. workspace.mjs + early GET-only handler in api.mjs). Serve only named public
   assets with content types/CSP; never arbitrary filesystem paths. Configuration
   uses relative /api/messages service base and can move with service. No credentials.
3. Add meaningful contract tests (not cosmetic duplicates), run existing API tests
   and new contract checks inside services/messages with `npm test`. Local HTTP
   tests require sandbox escalation to bind localhost. Start preview via existing
   Node service or a bounded local static server, navigate Chrome to /discussions/.
4. Inspect desktop/mobile in native Chrome, exercise participant selection,
   preparation, edits, reload persistence, empty validation and disabled controls.
   Native device toolbar via Chrome DevTools can provide mobile viewport. Verify
   real screenshots/AX state, no invented evidence.
5. Deploy new immutable service release following OPERATIONS.md. Include public
   assets in release, preserve /etc/q7x-messages.env. Run all tests on host Node 22.
   Point current to new release; restart only q7x-messages. Patch Caddy apex block
   with exact /discussions and /discussions/* proxy to 172.18.0.1:3320, validate,
   back up, preserve inode, reload. Existing API stays unchanged functionally.
   Add small /discussions/ link to original homepage with backup/concurrent-write
   guard; preserve its content and inode. Do not replace whole Caddy config.
6. Verify public page/assets and original API auth/read availability. Commit/push
   safely (no rewrite); record real evidence in child, close ONLY Q7C-733. Report
   live URL, commit, remaining placeholders to origin task
   `01a09d97-3c6b-7c93-9555-0e0ca74b4856` using send_message_to_thread.

Update this checkpoint after each meaningful phase and keep short recovery notes
in Q7C-733. No credentials in source, logs, checkpoints or browser storage.
